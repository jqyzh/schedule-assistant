# 更新日志

记录本项目从创建到现在的所有功能更新。

## 2026-09-12 — v1.0 初始版本 + 功能改进

### 一、初始版本 v1.0

- 创建项目结构：
  - `main.py` 程序入口（菜单 + 交互）
  - `schedule.py` 日程类与业务逻辑（Schedule / ScheduleManager）
  - `database.py` JSON 文件读写
  - `data.json` 数据文件
  - `README.md` 项目说明
- 实现核心功能：添加 / 查看 / 修改 / 删除 / 退出
- 数据使用 JSON 存储，关闭程序后不丢失

### 二、功能改进（按添加顺序）

1. **添加日程支持「回车取当前时间」**
   - 日期直接回车 = 使用今天
   - 时间直接回车 = 使用现在时刻

2. **查看日程新增「按编号查看单条」**
   - 查看菜单由 2 项变为 3 项：查看全部 / 按日期查询 / 按编号查看单条

3. **日期 / 时间格式统一**
   - 自动补零：`2026-9-12` → `2026-09-12`，`9:5` → `09:05`
   - 按日期查询时自动统一格式，避免格式不一致导致查不到

4. **全流程支持「0 返回上一级」**
   - 任意子菜单输入 `0` 可取消当前操作、退回上一级，不保存误输入

5. **修改日程支持三种内容修改方式**
   - 覆盖：用新内容替换
   - 追加：在原有内容后面加
   - 清空：删除原有内容

### 三、修改过的文件

- `main.py`：菜单交互、输入校验、返回机制、内容修改方式
- `schedule.py`：modify 方法支持清空（区分 None 与 ""）、按日期查询归一化
- `database.py`：初始版本后未改动
