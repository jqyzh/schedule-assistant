# 更新日志

记录本项目从创建到现在的所有功能更新。

## 2026-09-12 — v1.0 初始版本 + 功能改进

### 一、初始版本 v1.0

- 创建项目结构：
  - `main.py` 程序入口（菜单 + 交互）
  - `schedule.py` 日程类与业务逻辑（Schedule / ScheduleManager）
  - `database.py` JSON 文件读写
  - `data.json` 数据文件
  - `README.md` 项目说明
- 实现核心功能：添加 / 查看 / 修改 / 删除 / 退出
- 数据使用 JSON 存储，关闭程序后不丢失

### 二、功能改进（按添加顺序）

1. **添加日程支持「回车取当前时间」**
   - 日期直接回车 = 使用今天
   - 时间直接回车 = 使用现在时刻

2. **查看日程新增「按编号查看单条」**
   - 查看菜单由 2 项变为 3 项：查看全部 / 按日期查询 / 按编号查看单条

3. **日期 / 时间格式统一**
   - 自动补零：`2026-9-12` → `2026-09-12`，`9:5` → `09:05`
   - 按日期查询时自动统一格式，避免格式不一致导致查不到

4. **全流程支持「0 返回上一级」**
   - 任意子菜单输入 `0` 可取消当前操作、退回上一级，不保存误输入

5. **修改日程支持三种内容修改方式**
   - 覆盖：用新内容替换
   - 追加：在原有内容后面加
   - 清空：删除原有内容

### 三、修改过的文件

- `main.py`：菜单交互、输入校验、返回机制、内容修改方式
- `schedule.py`：modify 方法支持清空（区分 None 与 ""）、按日期查询归一化
- `database.py`：初始版本后未改动

## 2026-09-16 — 新增单元测试 + 项目规划与路线调整

### 一、新增单元测试（test_schedule.py）

- 使用 unittest 为 `ScheduleManager` 编写了 9 个单元测试，运行方式：`python -m unittest test_schedule -v`
- 测试用临时文件顶替 `data.json`，不触碰真实数据（setUp 生成、tearDown 删除）
- 覆盖场景：
  1. 添加日程
  2. 删除日程
  3. 删除不存在的编号（返回 None、不崩溃）
  4. `modify` 只修改传入的字段（None 表示不改）
  5. `event` 传 None 表示保持、传 "" 表示清空（区分 None 与 ""）
  6. 按日期查询自动补零（`2026-9-5` 也能查到 `2026-09-05`）
  7. `get` 越界返回 None、不崩溃
  8. 持久化：存盘后重新 load，数据仍在

### 二、后续升级方向调整（README）

- v2.0 GUI 界面方案由 **Tkinter/PyQt** 改为 **kivyMD**

### 三、新增项目规划文档（项目发展与规划.docx）

- 整理日期 2026-09-15，确定「AI 日程桌宠机器人」的整体规划
- 核心愿景：云端 AI 中枢 + 多端设备（一个大脑、多个身体）
- 技术栈选型：AI 用 DeepSeek API、ASR 用 Vosk、TTS 用 Piper、UI 用 kivyMD、数据库用 SQLite、定时提醒用 schedule 库
- 代码架构：AI 中枢与设备身体解耦（ai_brain.py / robot_body.py / schedule_db.py / reminder.py / ui.py / main.py）
- 开发排期：6-8 周四个阶段（搭骨架 / 长心脏 / 长脸 / 穿衣服）
- 长期路线图：大二上桌宠 → 寒假 STM32 温控 → 大二下实习

### 四、修改过的文件

- `test_schedule.py`：新增（单元测试）
- `README.md`：v2.0 GUI 路线调整（Tkinter/PyQt → kivyMD）
- `项目发展与规划.docx`：新增（项目规划文档）
- 其余代码（main.py / schedule.py / database.py）：本轮无改动
